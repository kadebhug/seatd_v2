#!/usr/bin/env bash
set -euo pipefail

command -v docker >/dev/null 2>&1 || {
  printf 'error: docker is required for infra bootstrap\n' >&2
  exit 1
}
docker compose -f infra/compose.yml config >/dev/null
