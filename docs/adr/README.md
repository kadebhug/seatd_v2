# Architecture decision records

ADRs capture decisions that affect multiple components, constrain future work,
or are costly to reverse. Use `0000-template.md`, assign the next sequential
four-digit identifier, and open the ADR in the same change as its implementation.

Statuses are `Proposed`, `Accepted`, `Deprecated`, or `Superseded by ADR-NNNN`.
