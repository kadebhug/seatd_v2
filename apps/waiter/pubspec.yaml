name: seatd_waiter
description: Seatd waiter application skeleton.
publish_to: none
version: 0.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  connectivity_plus: ^6.1.4
  drift: ^2.28.1
  flutter:
    sdk: flutter
  flutter_secure_storage: ^9.2.4
  http: ^1.5.0
  local_auth: ^2.3.0
  sqlite3_flutter_libs: ^0.5.39
  seatd_client:
    path: ../../packages/dart-seatd-client
  uuid: ^4.5.1
  web_socket_channel: ^3.0.3

dev_dependencies:
  build_runner: ^2.7.0
  drift_dev: ^2.28.1
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
